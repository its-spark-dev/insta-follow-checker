# Instagram login credentials (DO NOT share actual values here)

# Instagram username (used for session file name: session-<username>)
INSTA_USERNAME=your_username_here

# Optional: Custom session directory (default is ~/.config/instaloader)
SESSION_DIR=~/.config/instaloader
